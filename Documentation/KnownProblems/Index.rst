.. include:: ../Includes.txt


.. _known-problems:

==============
Known Problems
==============

.. important::

    Since the Localizer uses the TYPO3 CLI interface to avoid PHP limits while executing L10nmgr commands, **you have to make sure that the PHP version of your CLI meets the same minimum requirements** as the one used for your TYPO3 backend.